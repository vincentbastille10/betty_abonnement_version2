{
  "version": 2,
  "builds": [
    { "src": "api/ping.py", "use": "@vercel/python", "config": { "runtime": "python3.12" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/ping.py" }
  ],
  "installCommand": "pip install -r requirements.txt"
}
